import PageHeader from "@/components/layout/page-header"

// Shared by the page, its loading state and its error state
const TeamEmailHeader = () => (
  <PageHeader
    title="Email center"
    description="Every email you've sent to leads, newest first."
  />
)

export default TeamEmailHeader
